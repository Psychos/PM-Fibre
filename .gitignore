# Secrets — ne jamais committer
server/.env
*.env
local.properties

# Outils et caches de build
_tools/
.gradle/
.kotlin/
build/
app/build/

# Divers
*.log

# Documentation Claude Code (usage interne, pas destinée au dépôt public)
CLAUDE.md
AGENTS.md

# APK distribuées (bloat binaire ; distribution réelle via mapm.online)
*.apk
